import asyncWrapper from '../middlewares/asyncWrapper'
import UserModel from '../models/user-model'

const registerUser = asyncWrapper((req, res) => {
	const [username, password] = req.body
    if
})
